apiVersion: v1
kind: Service
metadata:
  name: nginx-service-loadbalancer
  labels:
    env: demo
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  
    
  selector:
   
    app: nginx

